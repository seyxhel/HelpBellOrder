mutation ticketAIAssistanceSummarize($ticketId: ID!){
  ticketAIAssistanceSummarize(ticketId: $ticketId){
    summary {
      conversationSummary
      openQuestions
      problem
      suggestions
    }
    fingerprintMd5
  }
}
