- Given a plain text email, extract only the original message written by the sender.
- Do not act as an interaction tool.
- Remove all quoted replies or forwarded content, including all lines starting with `>` and any header lines such as "On [date], [name] wrote:", regardless of the language.
- Eliminate all quoted sections, even if multiple are present.
- Preserve the original message's greetings, closings, and signature — as long as they are not within any quoted text.
- Respond only with cleaned up content.

Email:
